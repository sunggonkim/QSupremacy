\section{Evaluation}

This section evaluates \SystemName in the same style as a systems performance paper: first the setup, then the end-to-end application comparison, then baseline sensitivity, Perlmutter scalability, and advantage analysis. The result is not a claim that quantum wins today. The result is a measured threshold model that explains how fast and how accurate the future quantum path must be to beat native HPC paths.

\subsection{Evaluation Setup}

Experiments run on NERSC \Perlmutter. Login-node smoke tests validate correctness, while performance experiments run through Slurm. The first expanded sweep uses \texttt{gpu\_shared} jobs with one A100 GPU per job. Each job records JSON outputs, Slurm metadata, elapsed time, and allocation information.

\begin{table}[t]
\caption{Measured setup for the expanded digits sweep.}
\centering
\small
\begin{tabular}{ll}
\toprule
Item & Value \\
\midrule
System & NERSC Perlmutter \\
GPU & NVIDIA A100, 1 GPU per chunk \\
cuQuantum & 26.01.0 \\
Dataset & \texttt{sklearn digits} \\
Class pairs & 0-vs-1, 3-vs-8, 4-vs-9, 5-vs-8 \\
Samples & 128, 256 \\
PCA dimensions & 4, 8, 12, 16 \\
Native models & Logistic regression, MLP \\
Quantum models & Quantum kernel, QNN/VQC \\
Cases & 160 \\
\bottomrule
\end{tabular}
\label{tab:eval-setup}
\end{table}

\noindent\textbf{Evaluation questions.}
Table~\ref{tab:eval-questions} summarizes the evaluation flow. Each question maps to a measured table or figure, following the comparison--scalability--analysis structure used by our prior systems papers.

\begin{table}[t]
\caption{Evaluation roadmap.}
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lll}
\toprule
Question & Goal & Main evidence \\
\midrule
RQ1 & Compare native ML and quantum-circuit ML & Table~\ref{tab:expanded-summary}, Fig.~\ref{fig:digits-results} \\
RQ2 & Test quality sensitivity & Table~\ref{tab:class-sensitivity} \\
RQ3 & Cover practical application families & Table~\ref{tab:practical-suite}, Fig.~\ref{fig:practical-suite} \\
RQ4 & Stress the native baseline & Fig.~\ref{fig:strong-native} \\
RQ5 & Scale on Perlmutter GPU nodes & Table~\ref{tab:scaling}, Fig.~\ref{fig:scaling-summary} \\
RQ6 & Project advantage regions & Fig.~\ref{fig:advantage-frontier}, Fig.~\ref{fig:workload-taxonomy} \\
\bottomrule
\end{tabular}
}
\label{tab:eval-questions}
\end{table}

\subsection{RQ1: Native ML versus Quantum-Circuit ML}

Table~\ref{tab:expanded-summary} summarizes the expanded sweep. Native ML remains strong across the workload. Quantum kernels sometimes reach high quality, but the measured circuit path still requires hundreds of times faster projected execution to match the native path. QNN/VQC has a smaller required speedup in many cases, but its quality is usually below the native baseline.

\begin{table}[t]
\caption{Expanded digits sweep summary over 160 cases.}
\centering
\small
\begin{tabular}{lccc}
\toprule
Metric & Min & Median & Max \\
\midrule
Native accuracy & 0.9219 & 1.0000 & 1.0000 \\
Quantum kernel accuracy & 0.5312 & 0.8750 & 1.0000 \\
QNN/VQC accuracy & 0.4688 & 0.7500 & 0.9531 \\
Kernel required speedup & 338.6$\times$ & 421.9$\times$ & 1038.7$\times$ \\
QNN/VQC required speedup & 21.1$\times$ & 64.9$\times$ & 171.7$\times$ \\
\bottomrule
\end{tabular}
\label{tab:expanded-summary}
\end{table}

\noindent\textbf{Answer.}
The first result supports the main thesis: the useful output is not ``quantum is faster'' or ``quantum is slower.'' The useful output is the hardware requirement. For this workload, even when quantum kernels achieve good accuracy, the projected execution must improve by a median 421.9$\times$ to reach native time. For QNN/VQC, the median threshold is 64.9$\times$, but the model usually fails to match native accuracy.

\begin{figure}[t]
\centering
\begin{subfigure}{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/digits_required_speedup.pdf}
    \caption{Threshold distribution.}
    \label{fig:digits-speedup}
\end{subfigure}
\hfill
\begin{subfigure}{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{figures/digits_quality_speedup.pdf}
    \caption{Quality vs. threshold.}
    \label{fig:digits-quality}
\end{subfigure}
\caption{Expanded digits results over 160 cases. The quantum-kernel path often reaches useful accuracy but needs hundreds of times faster execution, while QNN/VQC has a lower threshold but usually lower quality.}
\label{fig:digits-results}
\end{figure}

\subsection{RQ2: Quality Sensitivity}

Table~\ref{tab:class-sensitivity} shows that the threshold depends on the data pair. The easy pair, 0-vs-1, gives high quantum-kernel accuracy. Harder pairs such as 3-vs-8 and 5-vs-8 reduce quality and make a simple speedup claim invalid. This is exactly why \SystemName keeps quality and runtime together.

\begin{table}[t]
\caption{Class-pair sensitivity. Values are medians.}
\centering
\small
\begin{tabular}{lcc}
\toprule
Class pair & Kernel acc. & QNN/VQC acc. \\
\midrule
0-vs-1 & 0.9688 & 0.8750 \\
3-vs-8 & 0.8125 & 0.7266 \\
4-vs-9 & 0.9375 & 0.7812 \\
5-vs-8 & 0.8125 & 0.6562 \\
\bottomrule
\end{tabular}
\label{tab:class-sensitivity}
\end{table}

\noindent\textbf{Answer.}
Quality changes the interpretation of speed. A lower runtime threshold does not imply advantage if the quantum model misses the native target quality.

\subsection{RQ3: Practical Application Suite}

The digits workload is useful because it is controlled, cheap, and easy to repeat. It is not enough for a practical quantum supremacy claim. Real users care about broader applications: AI pipelines, molecular energy estimation for drug discovery, optimization, and scientific simulation. These workloads have different native baselines and different quantum overheads.

\SystemName also evaluates a practical suite beyond the controlled binary digits sweep. The suite covers ML classification, VQE-style chemistry, QAOA-style optimization, and Hamiltonian simulation. These workloads represent the application classes where users often expect quantum advantage, but the measured result shows that the threshold is application dependent and usually large. The main practical-suite result below uses the large 3,552-case profile on 32 GPU nodes. ML selects among six native classifiers, and optimization selects among exact, greedy, local-search, and annealing baselines. Chemistry and simulation use native numerical baselines on the same Hamiltonian instances and compare them with quantum-circuit executions simulated through \cuQuantum.

\begin{table}[t]
\caption{Large practical suite summary over 3,552 cases. Values are medians.}
\centering
\small
\begin{tabular}{lccc}
\toprule
Workload & Cases & Required speedup & Quality gap \\
\midrule
ML & 2,048 & 3,726.4$\times$ & 0.3125 \\
Chemistry & 224 & 42,491.4$\times$ & 0.0203 \\
Optimization & 768 & 287,045.6$\times$ & 0.2500 \\
Simulation & 512 & 3,071.0$\times$ & 0.0188 \\
\bottomrule
\end{tabular}
\label{tab:practical-suite}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/practical_suite_summary.pdf}
\caption{Large practical suite over 3,552 cases. Each bar compares the selected native HPC-style baseline with a quantum-circuit application path simulated through cuQuantum. All families currently require faster projected quantum execution, but the threshold differs by application class.}
\label{fig:practical-suite}
\end{figure}

\noindent\textbf{Answer.}
The practical suite confirms that the gap is not an ML-only artifact. Every family remains outside the current advantage region, but the reason differs by family. Chemistry and simulation are often closer in quality but still require large execution improvement, while ML and optimization are frequently quality-limited. The range is also large: the median threshold is 3,071.0$\times$ for simulation, 3,726.4$\times$ for ML, 42,491.4$\times$ for chemistry, and 287,045.6$\times$ for optimization.

\subsection{RQ4: Native Baseline Stress Test}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/strong_native_comparison.pdf}
\caption{Native baseline stress test. Strengthening the native side makes the ML threshold 6.6$\times$ larger and the optimization threshold 2.3$\times$ larger than the initial native-baseline sweep. This confirms that \SystemName must treat native-baseline strength as part of the advantage model.}
\label{fig:strong-native}
\end{figure}

\noindent\textbf{Answer.}
The native baseline is part of the model. Strengthening the native side increases the ML threshold by 6.6$\times$ and the optimization threshold by 2.3$\times$ relative to the initial native-baseline sweep. The strong-native run completed as a one-node, four-GPU bundled allocation on \Perlmutter. The job finished 190 cases in 6 minutes and 59 seconds with exit code 0:0, 190 raw JSON outputs, and empty stderr logs. The selected ML baselines were RBF kernel ridge, nearest centroid, kNN, softmax regression, and linear ridge depending on the case; the selected optimization baselines were mostly greedy assignment, with exact enumeration selected when it was fastest at the best cut value.

\noindent\textbf{Baseline coverage gate.}
We also run a separate accept-profile gate to strengthen the two application families that are easiest to criticize as toy baselines: chemistry and scientific simulation. The chemistry gate includes OpenFermion/PySCF-generated H$_2$, LiH, and H$_2$O active-space Hamiltonians, in addition to the built-in minimal H$_2$ and molecular-chain surrogate. The committed fixture set now includes LiH and H$_2$O active spaces at 4, 6, and 8 qubits; the 6--8 qubit fixtures pass a login-node smoke gate before larger allocation runs. The simulation gate compares dense exact evolution with sparse Krylov evolution on TFIM and Heisenberg Hamiltonians from 4 to 8 qubits.

\begin{table}[t]
\caption{Accept-profile baseline-coverage gate on one \Perlmutter GPU node.}
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccl}
\toprule
Workload & Cases & Required speedup & Quality gap & Selected native model \\
\midrule
Chemistry & 56 & 63,566.8$\times$ & 0.4468 & Dense exact: 56 \\
Simulation & 60 & 4,185.2$\times$ & 0.0237 & Dense exact: 26; sparse Krylov: 34 \\
\bottomrule
\end{tabular}
}
\label{tab:accept-baselines}
\end{table}

\noindent\textbf{Answer.}
Table~\ref{tab:accept-baselines} shows that the strengthened chemistry and simulation baselines execute correctly in the same bundled one-node run. The job completed 116 cases in 6 minutes and 5 seconds with exit code 0:0; each of the four A100 tasks completed 29 cases, and stderr logs were empty. For chemistry, both dense exact and sparse Lanczos/eigensolver candidates were evaluated for every case. The selected runtime baseline is dense exact for all 56 cases because these active-space Hamiltonians are still small, but the run now uses OpenFermion/PySCF instances instead of only a hand-written surrogate. For simulation, sparse Krylov becomes the selected native method in 34 of 60 cases, especially at 6--8 qubits, showing that the native path is no longer a single dense solver.

\noindent\textbf{Chemistry active-space coverage.}
We then run the expanded chemistry-only accept profile with the 4--8 qubit OpenFermion/PySCF fixture set. Job 55515248 completed 104 chemistry cases on one \Perlmutter GPU node in 5 minutes and 23 seconds with exit code 0:0, 104 raw JSON outputs, and empty stderr. Table~\ref{tab:chem-coverage} shows the larger active-space subset. Dense exact remains the selected runtime baseline because these fixtures are still small, but sparse Lanczos is the best-quality native method in 62 of 104 cases. This result upgrades the chemistry evidence from a login smoke to a completed GPU coverage gate, while still reporting the limitation that these are active-space instances rather than production drug-discovery scale.

\begin{table}[t]
\caption{OpenFermion/PySCF chemistry active-space coverage gate.}
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
Problem & Qubits & Cases & Required speedup & Quality gap \\
\midrule
LiH active & 6 & 12 & 21,847.9$\times$ & 0.3030 \\
LiH active & 8 & 12 & 821.5$\times$ & 0.6299 \\
H$_2$O active & 6 & 12 & 21,613.0$\times$ & 1.0365 \\
H$_2$O active & 8 & 12 & 788.0$\times$ & 1.0916 \\
\bottomrule
\end{tabular}
}
\label{tab:chem-coverage}
\end{table}

\subsection{RQ5: Perlmutter Weak and Strong Scaling}

The large-profile suite contains 3,552 application cases. We evaluate two scaling modes. In weak scaling, each GPU receives roughly 28 cases, so total work grows with the number of GPUs. In strong scaling, every run executes the same 3,552 cases and the chunk slots are striped across the available GPUs. Both modes use one Slurm task per A100 GPU and independent application cases; this is throughput scaling, not distributed single-circuit simulation.

\begin{table}[t]
\caption{Large-profile weak and strong scaling on \Perlmutter.}
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Mode & Nodes & GPUs & Cases & Elapsed \\
\midrule
Weak & 8 & 32 & 896 & 4:05 \\
Weak & 16 & 64 & 1,792 & 4:04 \\
Weak & 32 & 128 & 3,552 & 4:17 \\
Strong & 4 & 16 & 3,552 & 30:55 \\
Strong & 8 & 32 & 3,552 & 15:13 \\
Strong & 16 & 64 & 3,552 & 11:05 \\
Strong & 32 & 128 & 3,552 & 4:17 \\
\bottomrule
\end{tabular}
\label{tab:scaling}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/scaling_summary.pdf}
\caption{Large-profile scaling. Weak scaling keeps per-GPU work nearly fixed and increases total throughput. Strong scaling fixes the full 3,552-case workload and measures time-to-solution speedup relative to four nodes.}
\label{fig:scaling-summary}
\end{figure}

\noindent\textbf{Answer.}
All six scaling jobs completed with exit code 0:0. The generated raw JSON counts match the expected case counts: 896, 1,792, and 3,552 for the weak-scaling runs, and 3,552 for each strong-scaling run. No failed case, traceback, or timeout was recorded in the task logs. The weak-scaling results keep per-GPU throughput close to stable from 8 to 32 nodes. The strong-scaling result reduces time-to-solution from 30 minutes 55 seconds on four nodes to 4 minutes 17 seconds on 32 nodes, a 7.2$\times$ speedup over an 8$\times$ node increase. This validates the bundled multi-node execution path for the advantage-modeling workflow.

\subsection{RQ6: Advantage Frontier}

The practical-suite result gives one speed threshold per case, but a speed-only threshold is still incomplete. A practical advantage claim also needs comparable output quality. We therefore convert each measured case into an advantage frontier over two axes: projected quantum execution speedup and quality-gap recovery. A point is in the advantage region only if the projected quantum path is no slower than the selected native path and the remaining quality gap is within a small workload tolerance.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/advantage_frontier.pdf}
\caption{Advantage frontier over projected quantum speedup and quality-gap recovery. Color shows the fraction of measured cases in each family that would enter the advantage region under that hypothetical hardware and algorithmic improvement. The contour marks the 50\% frontier when it appears.}
\label{fig:advantage-frontier}
\end{figure}

\noindent\textbf{Answer.}
The frontier separates speed-limited and quality-limited workloads. Chemistry and simulation often have small quality gaps, so their frontier is mostly pushed right by runtime. ML has a lower runtime threshold in some cases, but the quality gap must shrink before speed alone becomes meaningful. Optimization is far from the advantage region on both axes for the current QAOA configuration. This is the central point of \SystemName: quantum advantage is a feasible region, not a single speedup slogan.

\subsection{Operational Stability}

The official practical sweep was first run as multiple shared-GPU jobs. We then validated a bundled allocation path using \texttt{salloc} with two A100 GPUs. This pilot is not a new advantage claim. It checks that \SystemName can run multiple chunks inside one allocation, keep the GPUs busy with independent workload cases, and produce the same summary format used by the official sweep.

\begin{table}[t]
\caption{Bundled \texttt{salloc} pilot over 96 cases. Values are medians.}
\centering
\small
\begin{tabular}{lccc}
\toprule
Workload & Cases & Required speedup & Quality gap \\
\midrule
ML & 54 & 615.7$\times$ & 0.2865 \\
Chemistry & 6 & 49,476.5$\times$ & 0.2446 \\
Optimization & 18 & 210,056.9$\times$ & 0.2500 \\
Simulation & 18 & 11,629.0$\times$ & 0.0250 \\
\bottomrule
\end{tabular}
\label{tab:salloc-pilot}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/salloc_pilot_comparison.pdf}
\caption{Official 190-case sweep versus the 96-case bundled \texttt{salloc} pilot. The pilot uses two A100 GPUs inside one allocation and reproduces the same qualitative conclusion: every workload family remains outside the current advantage region, but the required hardware improvement differs by family.}
\label{fig:salloc-pilot}
\end{figure}

The pilot completed in 6 minutes and 54 seconds with 96 raw JSON outputs and empty stderr logs. Its main systems result is operational: the correct bundled pattern is a single \texttt{srun} step with multiple tasks, where each task selects a chunk using \texttt{SLURM\_PROCID}. Launching multiple overlapping \texttt{srun} steps was less reliable because of step-level resource ownership.

\subsection{Hardware Projection Model}

The current projection uses measured circuit metadata to compute a required speedup relative to native runtime. The next version should convert this speedup into logical gate time, measurement time, shot throughput, and error-correction overhead. The output should be a heatmap that answers: for each workload family, how fast must quantum hardware be before the quantum path beats the native HPC path at the same quality?

\subsection{Bottleneck Taxonomy}

The main evaluation output is not that the simulator is slow. The simulator is the measurement tool. The paper should use the measurements to explain what kind of quantum hardware and algorithmic behavior would be necessary for advantage. For each workload family, \SystemName should report whether the path is currently speed-limited, quality-limited, encoding-limited, shot-limited, error-overhead-limited, or native-dominated.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/workload_taxonomy.pdf}
\caption{Primary bottleneck taxonomy for the strong-native practical suite. ML and optimization are quality-limited in the current circuit configurations. Chemistry is mostly speed-limited, while simulation splits between speed and quality limits.}
\label{fig:workload-taxonomy}
\end{figure}

Figure~\ref{fig:workload-taxonomy} shows this classification for the 3,552-case large run. ML is quality-limited in all 2,048 cases because the quantum feature path does not match the selected native classifier accuracy. Optimization is also quality-limited in all 768 cases because the current QAOA grid has a large objective gap. Chemistry has 64 speed-limited cases and 160 quality-limited cases. Simulation splits evenly, with 256 speed-limited cases and 256 quality-limited cases.

This classification is the practical insight. If a workload is speed-limited, faster logical gates or more shot parallelism may move it into the advantage region. If it is quality-limited, faster hardware alone is not enough. If it is native-dominated, the correct conclusion is that the native HPC baseline remains the better application path unless the quantum algorithm changes.

\subsection{Remaining Sensitivity Checks}

The current expanded sweep covers class pair, sample count, PCA dimension, circuit depth, and seed. The next experiments must add warmup separation, repeated trials, shot sensitivity, ansatz sensitivity, optimizer sensitivity, and error mitigation overhead. These are necessary because small circuit experiments can be dominated by initialization, Python dispatch, and circuit construction overhead.
