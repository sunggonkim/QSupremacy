# QSupremacy Plan

## Goal

Model practical quantum advantage as an architecture-guided application-level
break-even condition. The paper display system name is **QArchGauge**.

```text
native HPC/ML application path
vs.
same workload converted to a quantum-circuit application path,
simulated with cuQuantum, then projected to future quantum hardware
```

The paper should answer how fast, how parallel, and how accurate future quantum
hardware must be before each workload family can beat the native path. It should
also identify the computer-architecture bottleneck: logical gate throughput,
shot parallelism, error/quality recovery, host-control overhead, native baseline
strength, or algorithmic quality.

## Current Status

| Area | Status |
| --- | --- |
| Paper PDF | Built at `paper/main.pdf` |
| HPCA body budget | References start on page 12 |
| Main practical suite | 3,552 cases, 128 GPUs |
| 256-GPU fixed work | 3,552 cases in 261 seconds |
| 256-GPU larger-workload gate | 7,104 cases in 514 seconds |
| Workload coverage | ML, chemistry, optimization, scientific simulation |
| Previous-paper logic/style | JSON audits PASS |
| Evidence audit | PASS |
| Submission readiness | `SUBMISSION_READY`, warning count 0 |

## Critical Review Integration Map

The two inserted reviews converge on five acceptance-critical issues. The paper
should address them through the manuscript's normal logic, not as a rebuttal
list.

| Review pressure | Manuscript response |
| --- | --- |
| Projection model is too abstract. | Design must decompose `T_error` into compile/map/route/decode/magic-state/data-load/control/queue terms and define effective `P_shots` as a bounded system resource. Evaluation should read the speed axis as a microarchitecture target, not a simulator speedup slogan. |
| `P_shots`, recovery, and tolerance are underspecified. | Design must treat shot parallelism as limited by device, decoder, control, queue, and finite evaluations. Evaluation must include a tolerance-sensitivity figure and explain that quality recovery is a requirement axis, not a measured free improvement. |
| Native baselines may be too weak. | Baseline sections must state the strongest implemented auditable baseline, show the strong-native shift, and explicitly frame future SOTA native methods as moving the frontier rightward rather than invalidating the method. |
| Scaling and seven-second circuit paths need systems explanation. | Evaluation must report quantum-runtime IQRs, explain the fixed small-circuit orchestration floor, and clarify that 128--256 GPU plateau is independent-case throughput granularity rather than distributed single-circuit MPI/cuQuantum synchronization. |
| Algorithm/workload scope may bias conclusions. | Discussion must state fixed QAOA/QNN/VQC choices as auditable scope, explain that better ansatz or training trades quality recovery against more depth/evaluations/control overhead, and avoid extrapolating small active spaces into deployment-scale chemistry. |

Style target: follow the PreviousPapers pattern: design figure first, then
component procedure; evaluation figure first, then data and systems
interpretation; use observation boxes for synthesized insight rather than
tables of raw facts.

## Validation Commands

```bash
make -B -C paper
python3 scripts/audit_previous_paper_alignment.py
python3 scripts/audit_previous_paper_deep_trace.py
python3 scripts/audit_previous_paper_style.py
python3 scripts/audit_paper_evidence.py
python3 scripts/audit_submission_readiness.py
```

Expected result:

```text
paper/main.pdf: builds successfully
previous-paper alignment: ALIGNED_BY_COUNTS
previous-paper deep trace: PASS
previous-paper style audit: PASS
paper evidence audit: PASS
submission readiness: SUBMISSION_READY, warning_count 0
```

## Authoritative JSON Artifacts

| Artifact | Path |
| --- | --- |
| Evidence audit | `data/processed/perlmutter/paper_evidence_audit.json` |
| Submission readiness audit | `data/processed/perlmutter/submission_readiness_audit.json` |
| Artifact manifest | `data/processed/perlmutter/paper_artifact_manifest.json` |
| Previous-paper alignment metrics | `data/processed/perlmutter/previous_paper_alignment_metrics.json` |
| Previous-paper deep trace | `data/processed/perlmutter/previous_paper_deep_trace.json` |
| Previous-paper style audit | `data/processed/perlmutter/previous_paper_style_audit.json` |
| Main 128-GPU summary | `data/processed/perlmutter/practical_suite_strongnative_32node_large128c0c127_20260704060230_summary.json` |
| 256-GPU fixed-work summary | `data/processed/perlmutter/practical_suite_strongscale_64node_largefull_c0c255_20260705024742_summary.json` |
| 256-GPU larger-workload summary | `data/processed/perlmutter/practical_suite_strongnative_64node_large256c0c255_20260705024742_summary.json` |
| Advantage projection | `data/processed/perlmutter/practical_suite_strongnative_32node_large128c0c127_20260704060230_advantage_projection.json` |

## Remaining Paper Work

No additional leadership-system experiments are required for the current paper claims.
Before submission, replace the HPCA `NaN` submission number and recheck the final
conference template/instructions.




Summary
This paper presents QARCHGAUGE, a measurement and architecture-projection framework that defines practical quantum advantage as a same-task, same-quality break-even condition between a native HPC path and a quantum-circuit application path. Using cuQuantum-based simulation on a leadership-scale GPU cluster, the authors measure end-to-end costs and quality across controlled ML and a broad practical suite (ML, chemistry, optimization, simulation), then invert these measurements into an “advantage frontier” over projected logical execution speed, shot parallelism, and quality-gap recovery to identify workload-specific architectural bottlenecks and requirements.

Strengths
Technical novelty and innovation
Treats practical quantum advantage as an application-level, same-input/same-quality break-even problem, not a kernel or simulator microbenchmark, which cleanly reframes the question for systems and architecture.
Provides an explicit projection interface from measured circuit metadata to future-system levers (logical op speed, shot parallelism, error/overhead, quality recovery) and a bottleneck taxonomy (speed/quality/shot/native/encoding limited).
Introduces the “advantage frontier” over speedup and quality-recovery axes, making clear that speed alone is often insufficient without quality improvements.
Experimental rigor and validation
Large-scale, leadership-class evidence: 160-case controlled digits sweep and a 3,552-case practical suite across ML, chemistry, optimization, and simulation, plus chemistry/simulation coverage gates and scaling experiments up to 64 nodes.
Careful experimental hygiene: same-input/same-quality targets across paths, JSON logging of runtimes/metadata, Slurm accounting, separation of smoke tests vs. performance runs, warmup-separated repeat timing, and failure-as-data handling.
Baseline strengthening analysis (dense/sparse solvers; exact and heuristic optimization; multiple ML classifiers) and explicit acknowledgment that stronger classical baselines raise the bar.
Clarity of presentation
Clear problem framing and repeated reminders that this is not a claim of present-day quantum wins but a design-space and bottleneck analysis.
Figures communicate both runtime and quality dimensions, and classification results translate to actionable guidance.
Significance of contributions
Provides architecture-relevant, workload-specific guidance on what must improve first (speed vs. quality vs. shots) to make quantum paths competitive.
Offers a reproducible measurement/projection discipline that others can adopt to avoid apples-to-oranges comparisons and misleading speedup claims.
Weaknesses
Technical limitations or concerns
The hardware projection model aggregates many nontrivial costs into a single Terror term and abstracts “recovery” as a free parameter; no calibrated, end-to-end FT stack instantiation or validation is provided, which limits prescriptive precision.
The notion of parallel shot throughput (Pshots) is treated parametrically without grounding in device/decoder/queue limits; practical bounds and contention models are not explored.
Circuit sizes are relatively small (ML 4–16 qubits; chemistry/simulation active spaces at 4–8 qubits), potentially biasing conclusions about scaling and bottlenecks.
Experimental gaps or methodological issues
Native baselines, while nontrivial, still omit stronger domain SOTA (e.g., CNNs or boosted trees for ML, tuned MILP/domain-specific heuristics for optimization, tensor-network or projector QMC for simulation, CCSD(T)-class references for chemistry), likely rendering the reported thresholds conservative.
Quality tolerances (ϵw = 0.02 for ML/opt, 0.01 for chem/sim) are fixed and somewhat arbitrary; only a coarse sensitivity description is provided, not a thorough analysis across a range of tolerances.
QAOA and VQC settings appear constrained (fixed grids, bounded optimizers), which may overstate quality limitations relative to better-tuned ansätze or training methods.
The near-uniform “~7 s per circuit path per case” across families is surprising; a deeper breakdown and variance analysis would improve confidence that results aren’t dominated by orchestration or fixed overheads.
Clarity or presentation issues
Some formatting artifacts (equation typography, figure placeholders) and anonymization of the platform hinder precise replication and external calibration to hardware characteristics.
The mapping from measured quality gaps to the “recovery” axis is abstract; readers would benefit from concrete examples (e.g., mitigation/fault-tolerance levers and their plausible ranges).
Missing related work or comparisons
Limited engagement with resource-estimation tools and FT-stack studies that could more concretely instantiate the projection (e.g., calibrated surface-code models with decoder latency and magic-state budgets).
Classical algorithm baselines specialized to the structures of the benchmark circuits (e.g., tensor networks on low-treewidth circuits) are not integrated, which may affect the frontier for specific families.
Detailed Comments
Technical soundness evaluation
The same-input/same-quality discipline, explicit runtime breakdowns, and “failure as data” policy are sound and welcome; they reduce common benchmarking biases.
The execution model Texecute = Ns(D1 t1 + D2 t2 + Dm tm)/Pshots + Terror is a reasonable first-order lower bound but leaves much of the realism to Terror and Pshots. Without bounding these terms via device- or FT-specific models, quantitative conclusions about “how much” improvement is needed remain indicative rather than prescriptive.
The frontier concept over speed and recovery is technically appropriate to expose that runtime and quality jointly determine advantage; the bottleneck taxonomy provides actionable separation between speed-limited and quality-limited regimes.
Experimental evaluation assessment
Breadth: The 3,552-case practical suite and chemistry/simulation coverage gates meaningfully extend beyond toy ML. The weak/strong scaling experiments and larger workload gate establish that the workflow is robust at cluster scale.
Depth: Circuit sizes and problem scales are still small. The optimization (QAOA) and VQC/quantum kernel configurations are limited; while this supports auditable breadth, it risks biasing conclusions toward quality-limited outcomes.
Baselines: The strong-native run and inclusion of sparse solvers are good steps. However, omitting stronger domain SOTA likely understates the true break-even thresholds and may shift taxonomy for ML and simulation.
Statistics and sensitivity: The repeatability gate (low CV) is reassuring, but richer statistical reporting (confidence intervals for medians, interquartile ranges per family/setting) and explicit tolerance sweeps would strengthen claims. The surprisingly uniform ~7 s circuit-path cost across disparate workloads deserves a per-stage decomposition and variance analysis.
Comparison with related work (using the summaries provided)
The paper goes beyond simulator scaling works (e.g., ScaleQsim, AURORA-Q) by embedding simulators within end-to-end application pipelines and explicitly comparing to native baselines at matched quality, which is a meaningful systems contribution.
The results on QML align with broad benchmarking evidence that quantum kernels and VQCs rarely beat strong classical baselines in accuracy or time today (e.g., large-scale QML benchmarks and MNIST-focused studies). This work complements those by adding end-to-end costs and architectural projection.
Compared to Q‑READY (a model-driven feasibility pipeline), QARCHGAUGE provides concrete, large-scale measurement artifacts and an empirical frontier over speed/quality; integrating Q‑READY’s SysML-style models with QARCHGAUGE’s measured artifacts could be a compelling future direction.
The paper cites fault-tolerant resource-estimation literature but does not instantiate it; drawing from resource estimators to concretize Terror and tie “recovery” to mitigation/FT levers would materially strengthen the projection.
Discussion of broader impact and significance
The paper advances the community’s understanding of where to spend “the next architecture dollar” for practical advantage, moving discourse away from isolated speedup headlines toward workload-specific, bottleneck-aware planning.
By insisting on same-task, same-quality comparisons and by releasing structured artifacts (as claimed), it sets a methodological bar that could materially improve the quality of future quantum systems papers.
The taxonomy indicates nearer-term architectural value in fast logical ops and shot concurrency for simulation/chemistry-like workloads, and algorithm/quality improvements for ML/optimization, which can help steer both hardware and algorithm roadmaps.
Questions for Authors
How sensitive are the advantage frontiers and bottleneck taxonomy to the chosen quality tolerances (0.01/0.02)? Can you include a figure or table showing frontier shifts across a range of tolerances per family?
The projection uses abstract recovery R and lumped Terror. Can you provide at least one calibrated FT instantiation (e.g., surface-code with decoder latency and magic-state budgets) to ground these parameters for a subset of cases?
How did you set or bound Pshots in the projection, and how would realistic device/decoder queue limits change the frontier for the speed-limited families?
The “~7 s median circuit-path time per case” appears consistent across quite different workloads. Can you break down this time into encoding, circuit construction, simulator kernel, measurement, and orchestration for each family and comment on variance across cases?
For ML, why exclude stronger classical baselines such as CNNs or boosted trees? Do your conclusions (especially taxonomy) change if those are included?
How robust are optimization results to better QAOA configurations (parameter transfer, warm starts, layerwise training, or natural gradient)? Could you report a focused sensitivity on a smaller subset?
Can you share more details about the repository and artifacts (commit hash, anonymized bundle)? Are the raw JSON logs for the 3,552-case suite and the 104-case chemistry coverage gate available to reviewers?
How do you envision incorporating energy/cost metrics and scheduler/queue effects into the frontier, given SOSP’s interest in end-to-end systems tradeoffs?
Could you compare against classical specialized solvers (e.g., tensor networks for relevant circuit families) where applicable to ensure the “native baseline” reflects structure-aware classical methods?
How do you anticipate the frontier moving at larger qubit counts (beyond 8–16)? Are there any extrapolations you can make using gate-depth scaling trends in your measurements?
Overall Assessment
This is a timely and well-executed systems paper that reframes “quantum advantage” as a concrete, architecture-relevant, same-task/same-quality break-even analysis. The measurement rigor, breadth of workloads, and the advantage-frontier/bottleneck taxonomy are valuable contributions that can influence how both hardware and algorithm designers prioritize efforts. The primary limitations are the simplicity of the projection model (abstract Terror and recovery R without a calibrated FT stack), modest circuit/problem scales, and the exclusion of some stronger classical baselines and algorithmic variants that could shift thresholds. Nevertheless, the central claims are careful and well supported by extensive evidence; the work squarely targets a question of high importance to the SOSP community (hybrid systems, workflow orchestration, realistic performance modeling). I lean to accept, contingent on clarifying sensitivity to quality tolerances, adding at least one calibrated hardware/FT instantiation, and deepening the per-stage runtime breakdown to address the apparent uniform per-case circuit cost.



HPCA 2027 제출을 위한 "QARCHGAUGE" 논문 심층 평가 및 리뷰어 분석 보고서1. 서론 및 평가의 맥락다가오는 2027년 1월 말 미국 유타주 솔트레이크시티에서 개최될 예정인 제33회 IEEE International Symposium on High-Performance Computer Architecture (HPCA 2027)는 컴퓨터 아키텍처 분야에서 가장 영향력 있는 최상위 국제 학술대회 중 하나이다. HPCA는 전통적인 프로세서, 메모리, 스토리지 시스템 아키텍처뿐만 아니라, 양자 컴퓨팅(Quantum Computing), 초전도체(Superconducting), 신경망 컴퓨팅(Neuromorphic computing)과 같은 신흥 기술이 컴퓨터 아키텍처에 미치는 영향과 실제 컴퓨팅 시스템의 평가 및 측정 방법론을 핵심 주제로 다루고 있다. 특히, 양자 컴퓨터 아키텍처 분야는 최근 몇 년간 노이즈가 있는 중간 규모 양자(NISQ) 시스템을 넘어 결함 허용 양자 컴퓨팅(Fault-Tolerant Quantum Computing, FTQC) 시대로 나아가기 위한 마이크로아키텍처 설계, 실시간 디코딩, 자원 추정(Resource estimation) 연구가 활발히 진행되며 HPCA의 주요 트랙으로 확고히 자리 잡았다.본 보고서의 평가 대상인 "QARCHGAUGE: Architecture-Guided Modeling of Practical Quantum Advantage" 논문 초안은 이러한 HPCA 2027의 주제적 요구사항에 매우 정확하게 부합하는 연구 목적과 방법론을 제시하고 있다. 이 논문은 실용적 양자 우위(Practical Quantum Advantage)를 단순히 새로운 양자 알고리즘의 복잡도 감소나 특정 양자 프로세서의 단일 실행 시간 단축으로 정의하지 않는다. 대신, 양자 우위를 컴퓨터 아키텍처 설계 공간의 경계(Design-space boundary) 문제로 재정의하며, 미래의 양자 시스템이 고전 고성능 컴퓨팅(HPC) 시스템을 능가하기 위해 논리적 게이트 지연 시간(Logical gate latency), 샷 병렬성(Shot parallelism), 오류 보정 오버헤드(Error-correction overhead), 그리고 알고리즘적 품질 회복(Quality recovery) 측면에서 어떠한 조건을 만족해야 하는지를 역산(Invert)하여 모델링한다.과거 HPCA에 발표된 양자 컴퓨팅 벤치마크 관련 선행 연구들과 비교할 때, QARCHGAUGE의 접근법은 상당한 진일보를 보여준다. 예를 들어, HPCA 2022에 발표된 SupermarQ 벤치마크 스위트는 애플리케이션 수준의 지표를 사용하여 하드웨어에 종속되지 않는 성능 평가를 시도했으나, 미래 아키텍처의 구체적인 요구사항을 정량적으로 투영(Projection)하는 데에는 한계가 있었다. 반면 QARCHGAUGE는 탑 20위권의 리더십 규모 HPC 시스템(TOP500 leadership-scale HPC system) 환경에서 최대 32개의 GPU 노드를 동원하여 3,552개의 방대한 실용적 애플리케이션 케이스(머신러닝, 화학, 최적화, 시뮬레이션)를 시뮬레이션하고 프로파일링하였다. 이를 통해 도출된 '우위 경계선(Advantage Frontier)'과 '병목 현상 분류(Bottleneck Taxonomy)'는 아키텍처 설계자들에게 하드웨어 개발의 우선순위를 결정할 수 있는 나침반 역할을 제공한다.본 보고서는 HPCA 2027 프로그램 위원회(Program Committee)가 요구하는 엄격한 심사 기준을 바탕으로 QARCHGAUGE 논문 초안의 전체 구성, 연구 디자인의 강점 및 약점을 심층적으로 해부한다. 나아가, 3,552개 케이스에 달하는 대규모 평가(Evaluation) 섹션의 각 하위 항목별 개선점을 도출하고, 양자 아키텍처 및 시스템 벤치마킹 분야의 최고 전문가로 구성된 가상의 리뷰어 3인의 시각에서 5점 만점의 정량적 평가와 세부적인 논문 수정 지시 사항을 제시한다.2. 논문의 전체 구성(Overall Composition) 평가HPCA 제출 논문은 11페이지의 엄격한 분량 제한(참고문헌 제외) 내에서 연구의 동기, 방법론, 시스템 구현, 대규모 평가, 그리고 아키텍처적 통찰을 짜임새 있게 구성해야 한다. QARCHGAUGE 논문은 이러한 학술대회의 구조적 기대치를 충실히 따르고 있으며, 특히 문제 정의부터 결과 해석까지 논리적 흐름이 매우 자연스럽게 연결되어 있다.논문 섹션 (Section)주요 내용 및 구성적 특징HPCA 적합성 평가 및 분석I. Introduction문제 제기, 아키텍처적 질문 도출, QARCHGAUGE 프레임워크 소개 및 4가지 핵심 기여 요약.단순한 알고리즘 제안이 아닌, 아키텍처 설계 지침(Design guidance)을 목표로 한다는 점을 명확히 하여 HPCA 리뷰어의 관심을 끄는 강력한 서론이다.II. Background양자 회로 애플리케이션 경로, 반복 실행 구조, 네이티브 HPC 베이스라인, 그리고 임계값 모델 설명. 선행 연구(ScaleQsim, SupermarQ 등)와의 차별성 제시.시스템 아키텍처 관점에서 양자 애플리케이션을 단계별 오버헤드 분해(End-to-end breakdown)로 설명한 점이 탁월하다.III. DesignQARCHGAUGE 프레임워크의 상세 설계. 측정 파이프라인, 공유 워크로드 제어(동일 입력/품질), 네이티브 및 양자 경로 실행 규칙, 투영 모델(Projection model) 수식화.방법론의 재현성(Reproducibility)을 보장하기 위한 엄격한 통제 변인 설정과 JSON 기반 레코드 로깅 시스템 설계가 매우 시스템 논문답게 서술되었다.IV. Evaluation160개 ML 캘리브레이션 케이스 및 3,552개 실용 스위트 평가. 베이스라인 스트레스 테스트, 리더십 시스템 스케일링(4~64 노드), 프론티어 분석 및 병목 체계 분류.실험 규모와 깊이가 압도적이며, 6개의 하위 질문에 답하는 체계적인 구조를 갖추었다. 특히 병목 원인을 5가지로 분류한 결론 도출 과정이 논리적이다.V. Discussion연구의 위협 요인(Threats to Validity), 네이티브 베이스라인 강도의 한계, 알고리즘 민감도, 시뮬레이터와 물리 하드웨어 간의 차이점 한계 인정.방법론적 한계를 투명하게 공개하고 이것이 결과 해석에 미치는 영향을 논증하여 학술적 신뢰성을 크게 높였다.VI & VII관련 연구(Related Work) 및 결론(Conclusion). 단일 속도 향상 지표의 무용성을 지적하고 도메인별 향후 아키텍처 발전 방향 제시.연구의 최종 메시지를 아키텍처 커뮤니티의 언어로 명확하게 요약하며 성공적으로 마무리하였다.논문의 전체 구성에서 가장 돋보이는 강점은 기존 벤치마킹 논문들이 흔히 범하는 '데이터의 나열'을 지양하고, 측정된 결과가 아키텍처 변수(속도, 품질, 병렬성)와 어떻게 상호작용하는지를 서사적으로 풀어냈다는 점이다. 제3장(Design)에서 제시된 투영 엔진(Projection engine)은 측정된 시뮬레이션 시간을 미래 하드웨어의 요구 사항으로 변환하는 핵심 기제로 작용하며, 이는 제4장(Evaluation)의 Advantage Frontier 분석으로 완벽하게 이어진다.그러나 구성 상의 미세한 단점도 존재한다. HPCA는 마이크로아키텍처(Microarchitecture) 수준의 상세한 설계 논의를 중시하는 학회이다. 본 논문은 평가(Evaluation)에 너무 많은 지면을 할애한 나머지, 투영 모델의 핵심인 오류 보정 시스템($T_{error}$)의 마이크로아키텍처적 구성을 서술하는 지면이 부족하다. 제3장에서 짧게 언급된 표면 코드 거리($d$)나 사이클 타임($\tau_c$)과 같은 파라미터들이 제4장의 대규모 실험 결과와 유기적으로 결합되지 못하고 단절된 느낌을 준다. 이는 추후 HPCA 제출 전 섹션 간의 분량 조절을 통해 반드시 보완되어야 할 구조적 약점이다.3. 연구 디자인의 강점 (Design Strengths)QARCHGAUGE의 연구 디자인은 기존의 양자 컴퓨팅 벤치마킹 방법론들이 가지고 있던 근본적인 결함들을 체계적으로 극복하는 혁신성을 보여준다. 아키텍처 연구 관점에서 볼 때 본 연구가 지니는 핵심적인 디자인 강점은 다음과 같다.첫째, 비교 대상과 기준을 엄격하게 통제하는 '동일 입력, 동일 품질 타겟(Same input, same quality target)' 규율의 확립이다. 과거 수많은 양자 컴퓨팅 연구들은 새로운 양자 휴리스틱 알고리즘(예: QAOA, VQE)을 제안하면서 고전 알고리즘 대비 실행 속도가 빠르다고 주장해 왔다. 그러나 이러한 연구들의 대다수는 양자 회로가 생성한 해답의 정확도나 근사 비율(Approximation ratio)이 고전적인 휴리스틱 솔버가 산출한 고품질의 해답에 현저히 미치지 못함에도 불구하고 이를 간과하는 오류를 범했다. QARCHGAUGE는 워크로드 컨트롤러를 파이프라인의 최상단에 배치하여, 네이티브 HPC 경로와 양자 회로 경로가 완전히 동일한 데이터 셋과 분할(Split)을 사용하도록 강제한다. 더 나아가, 도메인별로 수용 가능한 허용 오차($\epsilon_w$)를 사전에 정의하고, 양자 경로의 결과물이 이 타겟 품질을 만족하지 못할 경우 아무리 속도가 빠르더라도 양자 우위를 달성한 것으로 인정하지 않는다. 이러한 엄격한 품질 기반 브레이크이븐(Break-even) 조건의 도입은 양자 성능 평가의 패러다임을 '단순 속도 경쟁'에서 '실질적 유용성 검증'으로 전환시키는 결정적인 강점이다.둘째, 엔드투엔드(End-to-End) 애플리케이션 파이프라인 전체를 분석 대상에 포함한 점이다. 많은 선행 연구들은 양자 프로세서 내부에서 일어나는 회로 커널(Circuit kernel)의 순수 실행 시간만을 벤치마킹 지표로 삼아왔다. 하지만 실제 애플리케이션 환경에서는 고전 데이터를 양자 상태로 변환하는 인코딩(Encoding) 과정, 하드웨어 토폴로지에 맞춘 컴파일 및 라우팅 오버헤드, 매 샷(Shot)마다 발생하는 측정(Measurement) 지연, 그리고 변분 양자 알고리즘(VQA)에서 필수적인 고전적 파라미터 최적화(Classical optimizer) 루프가 막대한 병목 현상을 유발한다. QARCHGAUGE는 수식 (1), (2), (3)을 통해 데이터의 전처리부터 후처리에 이르는 모든 시스템 오버헤드를 투명하게 로깅하고 합산한다. 이는 특정 양자 칩의 큐비트 충실도(Fidelity)가 아무리 완벽해지더라도, 호스트 CPU와 양자 가속기 간의 하이브리드 오케스트레이션(Hybrid orchestration) 인프라가 병목을 일으킨다면 실용적 우위는 불가능하다는 시스템 아키텍처적 통찰을 제공한다.셋째, 대규모 워크로드 다양성과 자동화된 파이프라인의 결합이다. 단일 알고리즘이나 몇 개의 장난감 문제(Toy problem)에 국한되지 않고, 머신러닝, 분자 화학, 조합 최적화, 해밀토니안 시뮬레이션이라는 양자 컴퓨팅의 4대 핵심 응용 분야를 모두 포괄하는 3,552개의 실용적 케이스 스위트를 구축하였다. 특히, OpenFermion이나 PySCF와 같은 업계 표준 라이브러리를 파이프라인에 통합하여 분자 해밀토니안을 직접 생성하고, 희소 행렬 기반의 크릴로프(Krylov) 진화 등 강력한 고전 수치 해석 베이스라인을 자동으로 평가하도록 설계한 점은 이 프레임워크의 엔지니어링 완성도가 극도로 높음을 증명한다. 이는 리더십 규모의 HPC 시스템 환경(최대 64 노드)에서 며칠에 걸친 대규모 스위프(Sweep)를 사람의 개입 없이 안정적으로 수행할 수 있게 하는 원동력이다.넷째, 벤치마크 결과를 미래 아키텍처 설계를 위한 '병목 현상 분류 체계(Bottleneck Taxonomy)'로 승화시켰다는 점이다. 측정된 데이터를 단순히 '양자 속도 향상 X배'라는 표면적 수치로 끝내지 않고, 아키텍처 디자이너들이 즉각적으로 활용할 수 있는 가이던스로 변환하였다. 논문은 각 워크로드가 우위 영역(Advantage region)에 진입하기 위해 어떤 장애물을 극복해야 하는지에 따라 결과를 속도 제한(Speed-limited), 품질 제한(Quality-limited), 샷 제한(Shot-limited), 네이티브 지배적(Native-dominated) 상태로 명확히 라벨링한다. 이 분류 체계는 하드웨어 엔지니어(게이트 속도 개선), 시스템 아키텍트(샷 병렬화 및 스케줄러 개선), 그리고 양자 알고리즘 연구자(Ansatz 품질 향상 및 오류 억제) 모두에게 독립적이면서도 명확한 연구 목표를 제시하는 강력한 언어가 된다.4. 연구 디자인의 약점 (Design Weaknesses)QARCHGAUGE의 측정 철학과 프레임워크 설계는 훌륭하지만, 미래 양자 컴퓨터의 하드웨어 특성을 추정하는 수학적 투영 모델(Projection model)과 이를 뒷받침하는 마이크로아키텍처적 디테일에는 컴퓨터 아키텍처 논문으로서 간과할 수 없는 몇 가지 치명적인 약점들이 존재한다. HPCA 리뷰어들은 벤치마크의 규모만큼이나 하드웨어 모델링의 물리적 타당성을 날카롭게 검증할 것이다.4.1. 마이크로아키텍처 디테일의 부재 및 $T_{error}$의 과도한 추상화논문의 제3장 D절에서 제시된 투영 엔진은 양자 하드웨어 실행 시간을 수식 (5)로 정의한다: $\Gamma_{execute} = \frac{N_s(D_1t_1 + D_2t_2 + D_mt_m)}{P_{shots}} + T_{error}$. 이 수식은 지나치게 1차원적이고 추상적인 선형 모델에 불과하다. 특히, $T_{error}$라는 단일 변수 안에 컴파일, 매핑, 라우팅, 피드백 메커니즘, 오류 완화(Error mitigation), 그리고 오류 정정(Error correction)의 모든 복잡한 오버헤드를 은닉해버린 것은 중대한 오류이다.결함 허용 양자 컴퓨터(FTQC) 아키텍처에서 논리적 큐비트 연산은 물리적 게이트 지연 시간의 단순 합으로 결정되지 않는다. 예를 들어, 표면 코드(Surface code) 기반의 아키텍처에서는 논리적 T-게이트를 수행하기 위해 시스템의 다른 위치에서 생성된 매직 상태(Magic state)를 현재 연산 중인 큐비트로 전송해야 한다. 이는 래티스 서저리(Lattice surgery)나 브레이딩(Braiding)과 같은 복잡한 라우팅 경로를 요구하며, 데이터 큐비트와 보조 큐비트 간의 엄청난 물리적 통신 병목을 유발한다.HPCA 2025에 발표된 LSQCA (Load-Store Quantum Computer Architecture) 연구가 잘 보여주듯, 최근의 양자 아키텍처 트렌드는 메모리 영역과 연산 영역을 분리하여 큐비트 밀도를 높이면서도 라우팅 오버헤드를 줄이는 복잡한 플로어플랜(Floorplan) 전략을 제안하고 있다. 하지만 QARCHGAUGE의 투영 모델은 이러한 공간적(Spatial), 통신적(Communication) 오버헤드와 실시간 신드롬 디코딩(Real-time syndrome decoding)이 유발하는 마이크로아키텍처 수준의 레이턴시를 전혀 수식에 반영하지 않았다. 논문 본문에 짧게 예시로 언급된 표면 코드 거리 $d=25$나 사이클 타임 $\tau_c=1\mu s$ 수치조차도 , 3,552개 케이스의 실제 결과 분석에는 정밀하게 연동되지 않고 단순 상수로 처리되었다. 이는 HPCA 리뷰어들로 하여금 해당 투영 모델이 물리적 현실성을 결여한 단순 선형 외삽법(Extrapolation)에 불과하다고 느끼게 만들 수 있다.4.2. 네이티브 베이스라인 강도의 시스템적 한계논문은 "가능한 가장 강력한 유효 네이티브 방법(Strongest valid native path)"을 비교 대상으로 삼아 벤치마킹의 타당성을 확보했다고 주장한다. 논문은 머신러닝 워크로드에 대해 scikit-learn 기반의 로지스틱 회귀, MLP, 선형 릿지(Ridge), k-Nearest Neighbors 등의 모델을 적용하였고, 최적화 워크로드에는 그리디(Greedy)나 시뮬레이티드 어닐링(Simulated annealing)을 적용했다.그러나 최신 데이터 센터나 리더십 스케일 HPC 환경의 기준에서 볼 때, scikit-learn의 기본 구현체나 전통적인 CPU 기반 수치 해석 알고리즘은 결코 '최고 수준의 베이스라인'이 아니다. 오늘날의 엔터프라이즈 환경에서는 정형 데이터 처리를 위해 다중 GPU 클러스터 기반의 XGBoost나 LightGBM 트리를 사용하며, 이미지 분류에서는 고도로 양자화(Quantized)되고 최적화된 합성곱 신경망(CNN)을 사용한다.QARCHGAUGE가 구축한 네이티브 경로가 NVIDIA A100 GPU의 텐서 코어(Tensor Core)를 100% 활용하는 수준의 고도로 최적화된 CUDA 가속 파이프라인을 사용한 것인지, 아니면 단순히 스크립트 기반의 CPU 파이썬 코드 실행에 머물러 있는지에 대한 시스템적 명시가 부족하다. 만약 고전 베이스라인이 호스트 CPU의 병렬 처리 능력이나 하드웨어 가속기를 한계까지 끌어올리지 못한 채 상대적으로 느린 속도로 실행되었다면, $T_{native}$ 값은 부풀려지게 되고 결과적으로 양자 하드웨어가 달성해야 할 임계값(Break-even point)이 지나치게 느슨해지는(Easy to beat) 치명적인 벤치마킹 오류가 발생한다. 이는 논문의 핵심 결론인 '요구되는 양자 속도 향상 배수'의 신뢰성을 근본적으로 흔들 수 있다.4.3. 상태 벡터 시뮬레이션 지연과 양자 하드웨어 병렬성의 혼동 위험QARCHGAUGE는 양자 회로의 동작을 측정하기 위한 도구로 cuQuantum 기반의 상태 벡터 시뮬레이터(State-vector simulator)를 사용한다. 이 접근법 자체는 양자 상태의 정확한 품질 갭(Quality gap)을 측정하기 위한 훌륭한 선택이다. 하지만 투영 엔진이 시뮬레이션에서 측정된 메타데이터를 기반으로 미래 하드웨어의 $T_{execute}$를 계산하는 논리적 연결 고리에는 주의가 필요하다.상태 벡터 시뮬레이션의 실행 시간은 큐비트 수가 증가함에 따라 지수적(Exponential)으로 메모리 대역폭을 소모하며 급증한다. 반면, 실제 물리적 양자 하드웨어는 큐비트가 아무리 많아지더라도 회로의 얽힘(Entanglement) 구조와 뎁스(Depth)에 따라 병렬적으로 텐서 게이트 연산을 수행하므로 실행 시간이 다항 시간적(Polynomial) 또는 선형적(Linear)으로만 증가한다. 논문은 메타데이터(Gate count, Depth, Shots)를 추출하여 이를 재계산한다고 서술하지만, 투영 과정에서 HPC 시뮬레이터 특유의 오버헤드(GPU 간 메모리 복사 지연, 파이썬 인터프리터 런타임 지연 등)와 물리적 양자 프로세서의 근본적 제약(제어 펄스 생성 시간, 신드롬 측정 주기 등)이 혼동되거나 오염(Contamination)될 가능성을 논문 내에서 철저하게 분리하고 증명하는 수학적 검증 과정이 부족하다.5. Evaluation 섹션의 심층 분석 및 세부 개선점논문의 제4장 'Evaluation'은 리더십 스케일 시스템에서 측정된 3,552개의 프로파일을 다루며, 본 연구의 실증적 중추를 담당한다. 데이터의 방대함과 측정의 일관성은 훌륭하나, 분석의 깊이와 시스템적 원인 규명 측면에서 아키텍처 논문다운 정교한 개선이 요구된다. 다음은 평가 섹션의 각 하위 항목별 현황 분석과 HPCA 심사 기준을 통과하기 위한 구체적인 개선안이다.5.1. B. End-to-End Threshold Landscape의 분석 및 개선현황 요약: 논문은 확장된 digits 스위프(160개 케이스)와 대규모 실용 스위트(3,552개 케이스)의 결과를 바탕으로 임계값 분포를 제시한다. 핵심 관찰 결과로, 양자 커널(Quantum Kernel) 경로는 고전적 모델과 유사한 높은 품질(정확도)을 달성할 수 있으나 네이티브 실행 시간을 따라잡기 위해 중앙값 기준 약 421.9배의 속도 향상이 필요하다는 점을 밝혔다. 반면 QNN/VQC 모델은 속도 차이는 64.9배로 더 작지만 품질 격차가 커서 속도 향상만으로는 우위를 달성할 수 없음을 그림 6과 그림 7의 다차원 산점도로 증명하였다. 도메인별로는 머신러닝과 최적화가 '품질'의 벽에 막혀 있고, 화학과 시뮬레이션은 매우 긴 실행 '속도'의 벽에 직면해 있다는 강력한 통찰을 제공한다.개선 요구사항:극단값(Outlier) 및 롱테일(Long-tail) 분포에 대한 아키텍처적 해부: 평가 섹션은 전반적으로 중앙값(Median)이라는 단일 대푯값에 의존하여 결과를 설명하고 있다. 최적화 워크로드의 중앙값 요구 속도 향상이 287,045.6배라는 수치는 인상적이지만, 이 분포 내에는 요구 속도가 $10^6$배에 달하는 극단적인 롱테일 케이스들도 다수 존재할 것이다. 컴퓨터 아키텍트는 왜 특정 알고리즘 인스턴스(예: 밀도가 매우 높은 QAOA 그래프 구조)에서 유독 임계값이 폭발적으로 증가하는지 알고 싶어 한다. 단순히 수치를 제시하는 것을 넘어, 회로 메타데이터(예: 2-큐비트 게이트의 밀도, CNOT 계층 수 증가 등)와 런타임 간의 상관관계를 보여주는 회귀 분석(Regression analysis) 지표나 병목 스파이더 웹 차트를 추가해야 한다.도메인별 허용 오차($\epsilon_w$) 설정의 정당성 보완: 현재 모델은 ML 및 최적화에 대해 0.02, 화학 및 시뮬레이션에 대해 0.01이라는 임의의 허용 오차를 적용했다. 이 작은 숫자들이 도메인 전문가들이 체감하는 실제 임계값(예: 양자 화학에서의 Chemical Accuracy인 $1.6 \times 10^{-3}$ Hartree)과 어떻게 매핑되는지, 이 오차 범위 내로 들어왔을 때 해당 산업군(예: 신약 개발, 배터리 소재 탐색)에서 진정한 상업적 가치가 창출될 수 있는지에 대한 논리적 연결 고리를 서술해야 벤치마크 지표의 권위가 선다.5.2. C. Component and Baseline Analysis의 분석 및 개선현황 요약: QARCHGAUGE는 자신들의 측정 베이스라인이 장난감 수준이라는 비판을 방어하기 위해 네이티브 베이스라인 강도에 대한 스트레스 테스트를 수행했다. 향상된 베이스라인(Sparse Krylov/Lanczos 알고리즘 등)을 적용하자 머신러닝의 속도 임계값은 6.6배, 최적화는 2.3배 더 높아지는 결과를 그림 9를 통해 보여주었다. 이는 네이티브 소프트웨어 스택 자체가 고정된 타겟이 아니라 양자 시스템과 지속적으로 경쟁하며 발전하는 '이동하는 표적(Moving target)'임을 입증한 탁월한 아키텍처적 분석이다.개선 요구사항:CPU vs GPU 연산 오버헤드의 교차 분석: 앞서 지적한 바와 같이, 강화된 베이스라인이 단일 스레드 CPU의 결과인지 다중 노드 GPU 클러스터의 병렬 연산 결과인지 아키텍처적 컨텍스트가 부족하다. 네이티브 알고리즘이 GPU 메모리 대역폭이나 캐시 계층 구조(L2/L3 Cache)를 어떻게 활용했는지 Roofline 모델을 기반으로 한 성능 한계 분석(Performance Bound Analysis) 곡선을 제공한다면, 논문의 아키텍처적 깊이가 한 차원 상승할 것이다.OpenFermion/PySCF 오버헤드 분리: 화학 및 시뮬레이션 워크로드에서 해밀토니안 인스턴스 생성을 위해 사용된 파이썬 기반 라이브러리들이 발생시키는 순수 고전 연산 오버헤드(예: 2전자 적분 계산)가 양자 회로 구성 시간에 포함되었는지 분리되었는지 명확한 로깅 테이블을 추가하여야 한다.5.3. D. Leadership-scale Scaling Analysis의 분석 및 개선현황 요약: 대규모 프로파일 워크로드(3,552개 케이스)를 리더십 시스템에서 확장성 테스트한 결과가 그림 11과 그림 12에 제시되어 있다. 약한 스케일링(Weak scaling) 환경에서는 8 노드에서 32 노드로 증가함에 따라 총 처리량이 노드 수에 비례하여 선형적으로 훌륭하게 증가함을 보여주었다. 그러나 3,552개 케이스를 분할하여 처리하는 강한 스케일링(Strong scaling) 실험에서는 4 노드에서 32 노드로 8배 자원을 늘렸음에도 속도 향상은 7.2배에 그쳤으며, 64 노드로 자원을 추가 투입했을 때는 실행 시간이 32 노드와 동일한 약 4분 20초 대에 머무르며 심각한 병목(Plateau) 현상에 직면했음을 투명하게 보고하였다.개선 요구사항:분산 스케일링 포화 메커니즘의 시스템적 규명: 64 노드에서 강한 스케일링이 실패한 원인에 대해 논문은 단 한 줄로 "오케스트레이션 오버헤드와 케이스별 불균형(Orchestration overhead and per-case variability)이 원인"이라고 서술하고 넘어간다. 컴퓨터 아키텍처 및 시스템 커뮤니티(HPCA/ISCA/MICRO)는 이러한 단답형 진단을 극도로 혐오한다. Slurm 기반 작업 스케줄링 시 마스터 노드에서 발생하는 패킷 병목 때문인지, MPI 통신 지연인지, 아니면 cuQuantum 내부의 상태 벡터 동기화 오버헤드 때문인지를 NVIDIA Nsight Systems와 같은 프로파일러 로그를 첨부하여 정량적인 시간 지연 차트(Gantt chart)로 규명해야 한다.양자 샷 병렬성($P_{shots}$)의 클러스터 매핑 전략: 현재의 시스템 실험은 '여러 개의 독립적인 워크로드를 여러 GPU에 스케줄링'하는 태스크 레벨의 병렬성을 보여준다. 하지만 미래의 양자 우위를 달성하기 위한 투영 모델에서는 단일 워크로드 내에서 수십만 번의 샷을 병렬로 처리하는 샷 레벨 병렬성($P_{shots}$)이 요구된다. 다중 노드 HPC 시스템 환경에서 샷 병렬화를 구현할 때 발생할 네트워크 대역폭(예: InfiniBand NDR 등) 요구 사항이나 시스템 아키텍처 제약을 분석하는 단락이 스케일링 섹션에 추가되어야 한다.5.4. E. Advantage Frontier and Bottleneck Analysis의 분석 및 개선현황 요약: 그림 13은 투영된 양자 속도 향상(X축)과 품질 격차 회복률(Y축)의 2차원 공간에서 워크로드 케이스들이 우위 영역(Advantage region)에 진입하는 프론티어 곡선을 제시한다. 시뮬레이션의 경우 90% 품질 회복 시 $10^4$배의 속도 향상만으로도 절반 이상의 케이스가 우위 영역에 진입하지만, 화학은 $10^5$배가 필요하며 머신러닝과 최적화는 완벽한 품질 회복 없이는 어떠한 속도 향상으로도 우위를 점할 수 없다는 결론(Figure 14, 15)을 도출했다. 이는 단순히 '빠른 양자 칩'을 만드는 것보다 에러 억제 기술과 품질 보존 인코딩 기술이 선행되어야 함을 증명한 훌륭한 시각화 자료이다.개선 요구사항:투영 축의 물리적 마이크로아키텍처 매핑: X축에 표기된 $10^3, 10^4, 10^5$라는 무차원 속도 배수(Dimensionless multiplier)는 너무 추상적이다. 컴퓨터 아키텍트 독자들을 위해 보조 X축(Secondary X-axis)을 상단에 추가하여, 이 배수가 달성되기 위해 물리적 하드웨어 수준에서 요구되는 스펙(예: 논리 게이트 사이클 시간 $10\mu s \rightarrow 1\mu s \rightarrow 100ns$)을 병기해야 한다. 그래야 아키텍처 엔지니어들이 프론티어 곡선을 자신의 회로 설계 제원과 직접적으로 연관 지어 해석할 수 있다.에너지 및 전력 소모(Energy and Power Consumption) 모델의 결합: 진정한 실용적 양자 우위는 Time-to-Solution뿐만 아니라 Energy-to-Solution 측면에서도 평가되어야 한다. 수백만 개의 큐비트를 유지하기 위한 극저온 냉각기(Cryostat)의 소비 전력, 실시간 신드롬 디코딩을 수행하는 상온 FPGA 랙의 전력 소모, 마이크로웨이브 제어 펄스 발생기의 에너지 비용 등을 현재의 A100 GPU 32대(약 수십 kW 소모)와 비교하는 에너지 투영(Energy Projection) 곡선이 프론티어 분석에 포함된다면 이 논문의 학술적 파급력은 극대화될 것이다.6. 리뷰어 3평의 입장에서 본 5점 만점 평가 및 세부 개선점HPCA 프로그램 위원회(PC)의 다양하고 엄격한 심사 기준을 대변하기 위해, 각기 다른 전문 분야를 가진 3인의 가상 리뷰어(Reviewer) 입장에서 본 논문에 대한 정량적 평가와 구체적인 수정 지시 사항을 작성하였다.6.1. Reviewer 1: 양자 컴퓨터 마이크로아키텍처 및 결함 허용(FTQC) 오류 정정 전문가평가 항목리뷰어 1의 채점 및 코멘트 요약점수 (5점 만점)Overall Score동일 입력/품질을 기반으로 한 참신한 평가 프레임워크나, 결함 허용 아키텍처의 물리적 복잡성을 지나치게 단순화함.3.5 / 5RelevanceHPCA 2027의 "Quantum architectures" 핵심 트랙에 매우 완벽히 부합함. 5 / 5Novelty단일 속도 향상이 아닌 2차원 공간에서의 Advantage Frontier 도출은 독창적임.4 / 5Methodology$T_{error}$의 추상화가 너무 강하여 마이크로아키텍처 모델로서의 디테일이 부족함.2.5 / 5[상세 리뷰 코멘트]
본 논문은 실용적 양자 우위를 추정하기 위해 엔드투엔드(End-to-End) 시스템 관점에서 네이티브 HPC 베이스라인과 양자 회로 경로를 1:1로 공정하게 비교한 훌륭한 시스템 논문입니다. 특히 벤치마킹 결과를 바탕으로 병목 현상을 논리적 연산 속도, 샷 병렬성, 품질 회복의 다차원적 지표로 분해한 점은 아키텍처 커뮤니티에 큰 시사점을 제공합니다.그러나 '아키텍처 가이드(Architecture-Guided)'라는 당찬 제목에 걸맞지 않게, 양자 하드웨어 모델링(Section III.D.1) 부분이 지나치게 추상적입니다. 저자들이 제안한 수식 (5)의 $T_{error}$는 컴파일, 매핑, 라우팅, 매직 상태 증류, 신드롬 디코더 레이턴시 등을 모두 뭉뚱그려 놓은 블랙박스에 불과합니다. 진정한 결함 허용 양자 컴퓨팅(FTQC) 마이크로아키텍처에서는 위상학적 오류 정정 코드(예: Surface code 또는 LDPC)를 적용할 때 논리적 큐비트 간의 통신(Routing) 및 T-게이트 주입(Magic state injection)에 막대한 공간적, 시간적 오버헤드가 발생하며, 이는 회로의 넓이(Width)와 거리에 따라 비선형적으로 요동칩니다.[세부 수정 지시 사항 (Action Items)]마이크로아키텍처 오버헤드 분해: 수식 (5)의 $T_{error}$를 단순히 추상적인 상수로 두지 마십시오. $T_{error}$를 $T_{routing} + T_{decoding\_latency} + T_{magic\_state\_prep}$ 등의 하위 마이크로아키텍처 요소로 세분화하는 수식을 본문에 추가해야 합니다. 논문 내 짧게 언급된 $d=25$, $\tau_c=1\mu s$ 예시를 구체화하여, 대규모 화학 워크로드에서 발생할 디코딩 병목을 평가 파트의 그래프 데이터로 연동하여 보여주십시오.QRAM 및 Load-Store 지연 시간 고려: ML 및 화학 데이터셋을 양자 상태로 변환하는 인코딩 과정($T_{encoding}$)은 향후 QRAM이나 LSQCA(Load-Store Quantum Computer Architecture) 모델과 같은 메모리-연산 분리 구조에 의해 지연 시간이 결정될 것입니다. 데이터 인코딩 오버헤드가 아키텍처 메모리 계층의 어느 부분에서 병목을 일으킬지에 대한 아키텍처적 가설을 명시하십시오.6.2. Reviewer 2: 고성능 컴퓨팅(HPC) 시스템, 스케일링, 벤치마킹 방법론 전문가평가 항목리뷰어 2의 채점 및 코멘트 요약점수 (5점 만점)Overall Score강력한 벤치마킹 통제 메커니즘과 평가 스케일은 훌륭하나 분산 스케일링 분석이 빈약함.4.0 / 5Relevance"Evaluation and measurement of real computing systems" 트랙 기준을 상회하는 규모.5 / 5NoveltySupermarQ 등 기존 벤치마크와 비교할 때 Same-quality 방법론의 엄밀함이 우수. 4 / 5Methodology강한 스케일링 포화 원인 및 네이티브 베이스라인의 SOTA 시스템 최적화 여부 검증 부족.3 / 5[상세 리뷰 코멘트]
"동일한 입력과 동일한 품질 타겟"을 기준으로 고전 네이티브 애플리케이션과 양자 시뮬레이션 간의 손익분기점(Break-even)을 분석한 QARCHGAUGE의 철학은 최근 HPCA, ISCA, MICRO 등에 쏟아져 나오는 여러 양자 벤치마킹 연구(SupermarQ, QASMBench 등) 중에서도 가장 엄밀하고 실용적인 시도입니다. 또한 최대 64개 GPU 노드에서 7,104개 케이스를 처리하며, 파이프라인의 웜업 분리 타이밍 검증(Warmup-separated repeat-timing gate)을 거친 것은 컴퓨터 시스템 평가 논문으로서 매우 모범적인 자질을 보여줍니다.다만 HPC 벤치마킹 논문의 엄격한 관점에서 볼 때 두 가지 아쉬운 점이 있습니다. 첫째, 64 노드 스케일링 실험에서 32 노드 이후 강한 스케일링이 포화 상태(Plateau)에 도달한 현상을 시스템 내부적으로 명확히 규명하지 않았습니다. 둘째, '강력한 고전 네이티브 베이스라인'이라는 주장이 최신 하드웨어 가속기(GPU Tensor Cores 등)를 100% 활용한 최적화 결과인지 불분명하여, 타겟 런타임($T_{native}$)이 인위적으로 느슨하게 설정되었을(Easy to beat) 우려가 있습니다.[세부 수정 지시 사항 (Action Items)]스케일링 병목의 시스템 레벨 프로파일링: 그림 12의 32 노드 병목 현상이 왜 발생했는지 추측성 서술에 그치지 말고, 시스템 프로파일러(예: NVIDIA Nsight Systems 툴 등)를 사용해 MPI/Slurm 메시지 전달 오버헤드, I/O 스토리지 접근 병목, 마스터-워커 간 동기화 지연 등의 요소를 정량적으로 규명한 파이 차트(Pie chart)나 간트 차트를 추가하십시오.SOTA 기반 고전 베이스라인의 투명성 확보: 네이티브 알고리즘(ML의 scikit-learn, 최적화의 그리디 알고리즘 등)이 단일 CPU 코어 실행을 넘어 멀티 노드 GPU 클러스터의 병렬 연산 및 텐서 코어를 극한으로 활용했는지 하드웨어 사용률(Utilization) 지표를 명시하십시오. 양자 컴퓨터가 이겨야 할 진정한 대상은 '단일 스레드 파이썬 코드'가 아니라 시스템 스택 전체를 최적화한 분산 고전 알고리즘이어야 합니다.6.3. Reviewer 3: 양자 알고리즘 및 소프트웨어-하드웨어 공동 설계(Co-design) 전문가평가 항목리뷰어 3의 채점 및 코멘트 요약점수 (5점 만점)Overall Score프레임워크의 개념과 병목 체계는 뛰어나나, 하드코딩된 양자 회로 구조가 결론을 편향시킴.3.5 / 5Relevance벤치마크 및 응용 사례 도출이라는 측면에서 학회의 요구사항에 잘 맞음.4 / 5Novelty워크로드 패밀리별 병목 체계 분류(Taxonomy) 제안은 매우 통찰력 있음.4 / 5Methodology고정된 깊이(Depth)와 탐색 방식이 양자 알고리즘 자체의 유연성을 억압함.2.5 / 5[상세 리뷰 코멘트]
도메인별(머신러닝, 화학, 최적화, 시뮬레이션)로 병목 현상을 속도, 품질, 샷 수 등의 측면에서 정량화하여 Taxonomy를 도출한 제4장 E절은 본 논문의 가장 빛나는 성과입니다. 특히 품질 회복률(Quality Recovery) 향상 없이는 머신러닝과 최적화 알고리즘이 결코 고전 컴퓨터를 이길 수 없음을 2차원 프론티어 차트로 입증한 점은 아키텍처 및 알고리즘 연구자 모두에게 훌륭한 교훈을 줍니다.그러나 양자 애플리케이션 파이프라인의 실행 파라미터가 지나치게 고정적(Rigid)이라는 근본적 약점이 존재합니다. 본 연구는 QAOA 레이어 수나 VQC 파라미터 계층을 고정된 그리드에서만 평가했습니다. 최신 알고리즘-하드웨어 공동 설계(Co-design) 기법은 하드웨어의 노이즈 특성이나 큐비트 토폴로지에 맞춰 Ansatz의 구조를 동적으로 변화시키는(Hardware-efficient Ansatz) 방향으로 진화하고 있습니다. 논문이 현재의 고정된 회로 구조만으로 "ML과 최적화는 품질 제한적(Quality-limited)이다"라고 단정 짓는 것은, 사용된 고정 Ansatz 자체가 충분한 표현력(Expressibility)을 갖지 못해 발생한 알고리즘적 한계를 마치 양자 아키텍처의 구조적 문제인 것처럼 편향되게 해석할 여지를 남깁니다.[세부 수정 지시 사항 (Action Items)]알고리즘적 유연성(Algorithmic Flexibility)에 대한 다차원 투영 분석: 품질 갭(Quality gap)을 강제로 줄이기 위해 회로의 깊이(Depth)나 파라미터 수를 동적으로 늘릴 경우, 실행 시간 $T_{qhw}$가 이에 비례하여 얼마나 악화되는지를 보여주는 트레이드오프(Trade-off) 궤적 곡선을 그림 13의 Advantage Frontier 위에 오버레이(Overlay)하여 제시하십시오.하이브리드 루프 오버헤드 시각화: QNN이나 QAOA와 같은 변분 알고리즘은 최적화 파라미터를 업데이트하기 위해 수천 번의 호스트 CPU-QPU 간 반복 통신을 수행해야 합니다. 현재 $T_{error}$나 후처리 과정 안에 숨겨져 있는 이 '고전-양자 통신 레이턴시(Classical-Quantum Communication Latency)' 오버헤드를 아키텍처적 병목 요인으로 분리하여 그림 8의 회로 연산 비중 차트에 명시적으로 추가하십시오.7. 최종 결론 및 요약 권고 (Conclusion and Recommendations)"QARCHGAUGE: Architecture-Guided Modeling of Practical Quantum Advantage" 논문 초안은 실용적 양자 우위를 엄밀하게 측정하고 검증하기 위한 훌륭한 시스템 소프트웨어적 뼈대와 방대한 규모의 실험 데이터를 성공적으로 구축하였다. 특히, 3,552개에 달하는 다양한 실용적 워크로드에 대해 동일 입력/동일 품질 타겟 기반의 비교 철학을 적용하고, 병목 현상을 4가지 축으로 분류한 접근법은 기존에 발표된 여타 양자 벤치마크 모델(SupermarQ, QASMBench 등) 대비 확실한 우위를 점할 수 있는 강력한 무기이다.그러나, 본 연구가 HPCA 2027이라는 최상위 컴퓨터 시스템 및 아키텍처 국제 학술대회에서 높은 점수로 최종 채택되기 위해서는(특히 "Quantum architectures" 트랙의 까다로운 기준을 통과하기 위해서는), 시뮬레이터와 소프트웨어 벤치마킹 관점에 다소 치우쳐 있는 현재의 텍스트 서술 방식을 마이크로아키텍처 스펙과 물리적 제약을 명시하는 진정한 하드웨어 아키텍처 관점으로 끌어내리고 고도화하는 작업이 필수불가결하다.저자들은 앞서 3인의 가상 리뷰어들이 공통으로 지적한 바와 같이, 오류 보정 메커니즘을 단순히 상수로 치환해버린 수학적 투영 모델을 디코더 지연 시간, 표면 코드 라우팅 오버헤드, 매직 상태 주입 시간 등으로 물리적으로 해체하여 분석해야 한다. 또한 분산 환경에서의 스케일링 포화 원인을 시스템 프로파일러로 규명하고, 네이티브 고전 베이스라인이 SOTA 하드웨어 가속 성능을 온전히 활용했음을 증명하는 절차를 평가 섹션에 반드시 보강해야 한다.위에서 제시된 깊이 있는 개선점들이 투영 모델 수식의 고도화와 Advantage Frontier 그래프의 다차원적 확장에 충실히 반영된다면, 본 논문은 불확실성이 만연한 양자 하드웨어 개발 로드맵에 체계적인 시스템 아키텍처적 가이드라인을 제시하는 기념비적인 기준(Baseline) 논문으로 인정받을 수 있을 것이다. 컴퓨터 아키텍처 커뮤니티의 관점에서 볼 때, 하드웨어의 불확실성을 데이터 기반의 벤치마킹과 병목 현상 투영 모델링을 통해 논리적으로 극복하고자 한 본 연구의 방향성은 학문적, 산업적 파급 효과가 매우 클 것으로 전망된다.
